# data/* and models/* removed from LFS - Railway does not pull LFS files.
# Run: git lfs migrate export --include="data/*,models/*" --everything
# to convert existing LFS files to regular commits before deploying.
experiments/mlruns/* filter=lfs diff=lfs merge=lfs -text
